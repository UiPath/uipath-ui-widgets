/**
 * Default URL path the DU Validation Station web-component bundle is served
 * under, and the widget's runtime loader default
 * (`configureValidationStationWc({ baseUrl })`).
 *
 * The `uipath-vs-wc copy-assets` CLI defaults to `public/du-vs-wc`, which most
 * frameworks serve at this same path — so the provisioned path and the loaded
 * path line up with no configuration.
 */
export const DU_VS_WC_BASE = "/du-vs-wc/";
